[target.aarch64-unknown-linux-gnu]
linker = "aarch64-linux-gnu-gcc"

[env]
# Point to Jetson's CUDA libraries for linking
# These will be used when cross-compiling for aarch64
CUDA_PATH = { value = "/usr/local/cuda", force = false }
