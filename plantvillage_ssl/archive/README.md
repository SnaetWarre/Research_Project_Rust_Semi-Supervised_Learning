# Archive Notice

The code in this directory is historical and not part of the active `plantvillage_ssl` build or runtime paths.

Use `plantvillage_ssl/gui/src-tauri` and the current root-level tooling for active development.
